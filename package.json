{
  "name": "ydrax-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site vitrine YDRAx (statique) et sa fonction de contact.",
  "dependencies": {
    "nodemailer": "^6.9.16"
  },
  "scripts": {
    "dev": "npx serve -l 3000 .",
    "test": "node --test test/*.test.mjs"
  }
}
